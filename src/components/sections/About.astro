---
import { Image } from "astro:assets";
import GrenEye from "../../images/ojo-verde.jpg";
import VallaxLogoLetters from "../../images/logo-letters.png";

const highlights = [
  {
    title: "Alertas inteligentes",
    description:
      "Define palabras clave, filtros de precio y ubicación para recibir solo los productos que encajan contigo.",
  },
  {
    title: "Multiplataforma",
    description:
      "Centraliza Wallapop, Vinted y Milanuncios en un único flujo de avisos listo para revisar en segundos.",
  },
  {
    title: "Reacciona primero",
    description:
      "Las notificaciones instantáneas en Telegram te dan ventaja para contactar antes que la competencia.",
  },
];
---

<section id="about" class="mt-10 md:mt-[100px] lg:mt-[132px]" aria-labelledby="about-title">
  <div class="grid gap-8 xl:grid-cols-[minmax(0,1fr)_minmax(0,360px)]">
    <div class="linear-bg-aux-3 space-y-6 border border-white/15 px-8 py-12 text-left lg:px-12 lg:py-16">
      <p class="text-xs font-semibold uppercase tracking-[0.35em] text-primary">¿Qué es Vallax?</p>
      <h3 id="about-title" class="text-3xl font-bold leading-[1.1] text-white lg:text-[42px]">
        Tu asistente para detectar chollos antes que nadie
      </h3>
      <p class="text-base leading-[28px] text-white/80">
        Vallax es la aplicación que te permite <strong>crear alertas personalizadas</strong> en
        <strong>Wallapop</strong>, <strong>Vinted</strong> y <strong>Milanuncios</strong>.
      </p>
      <p class="text-base leading-[28px] text-white/80">
        Recibe <strong>notificaciones al instante</strong> en <strong>Telegram</strong> de los artículos que más te
        interesan y actúa con ventaja en cada negociación.
      </p>
      <ul class="space-y-4">
        {highlights.map((item) => (
          <li class="flex items-start gap-3">
            <span class="mt-1 h-2.5 w-2.5 flex-none rounded-full bg-secondary"></span>
            <div>
              <p class="text-lg font-semibold leading-6 text-white">{item.title}</p>
              <p class="mt-1 text-sm leading-6 text-white/70">{item.description}</p>
            </div>
          </li>
        ))}
      </ul>
    </div>

    <div class="flex flex-col gap-4">
      <div class="overflow-hidden border border-white/15">
        <Image src={GrenEye} alt="Encuentra chollos gracias a Vallax" class="h-full w-full object-cover" />
      </div>
      <div class="linear-bg-aux-3 border border-white/15 px-6 py-8 text-center">
        <p class="text-xs uppercase tracking-[0.3em] text-white/60">Caza chollos</p>
        <p class="mt-3 text-2xl font-semibold leading-8 text-aux1">
          Monitoriza oportunidades en tus plataformas favoritas y responde al instante.
        </p>
      </div>
      <div class="flex items-center justify-center border border-white/15 px-6 py-8">
        <Image src={VallaxLogoLetters} alt="Logo Vallax" class="max-h-12 w-auto" />
      </div>
    </div>
  </div>
</section>
